<template>
    <div>
        <div>
            <h1>Things I need to do</h1>
        </div>

        <div>
            <ul v-if="todos.length">
                <li v-for="item in todos">
                    {{item.description}} – 
                    <button class="btn btn-link" v-on:click="completeItem(item)">
                        Completed
                    </button>
                </li>
            </ul>
            <p v-else class="text-success">Nothing to do right now!</p>
        </div>
        
        <div>
            <form>
                <div class="row">
                    <div class="col-6">
                        <input v-model="newItemDescription" class="form-control" placeholder="I just remembered that I have to...">
                    </div>
                    <button type="submit" class="btn btn-primary" v-on:click="addItem($event)"> Add </button>
                </div>
            </form>
        </div>
    </div>
</template>
<script src="./todos.ts"></script>